version: 0.2
phases:
  pre_build:
    comands:
      - echo "Pre Build"
  build:
    comands:
      - echo "Building"
      - mvn clean compile war:war
  post_build:
    comands:
      - echo "Movendo Arquivos"
      - mv target/*.war ./ROOT.war
artifacts:
  files:
    - ROOT.war